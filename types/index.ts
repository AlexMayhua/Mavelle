export interface NavItem {
  label: string;
  href: string;
}

export interface Product {
  title: string;
  description: string;
  icon?: string;
}

export interface Collection {
  title: string;
  description: string;
  tag: string;
}

export interface Feature {
  title: string;
  description: string;
  icon?: string;
}
